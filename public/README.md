# public/

Static assets served from the site root.

## Required files

- **`logo.png`** — the TrendFinder logo (square, e.g. 1024×1024). Used in the
  site header, the welcome popup, the favicon and Open Graph / social previews.
  Save the brand image here as exactly `logo.png`.
